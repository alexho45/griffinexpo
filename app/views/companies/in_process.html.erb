
<% if @company.new_record? %>
  <%= render "companies/steps/common/select_type" %>
<% else %>
  <%= render "companies/steps/#{@company.company_type}/#{@company.process_step}" %>
<% end %>

<%= "companies/steps/#{@company.company_type}/#{@company.process_step}" %>
