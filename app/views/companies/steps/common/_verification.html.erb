
<%= render 'companies/verification_info', locals: { company: @company } %>

<%= link_to 'BACK', confirmation_companies_path(step_back: true), method: :post, class: 'btn btn-primary' %>
<%= link_to 'NEXT', confirmation_companies_path, method: :post, class: 'btn btn-primary' %>
