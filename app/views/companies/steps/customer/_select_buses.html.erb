
<h4>EXPO EVENTS SCHEDULING</h4>

<h5>
  Would you require transportation to and from the EXPO to your hotel?
  <br>
  (<a data-toggle="modal" data-target="#bus-modal">Click here</a> to check schedule)
</h5>

<%= form_tag(select_buses_companies_path, method: :post) do %>
  <h4>
    <%= radio_button_tag "checked_buses", true, checked = session.has_key?(:buses_selected) ? session[:buses_selected] : true, class: 'checked_buses' %> YES
  </h4>
  <%= hidden_field_tag :attendees_count, @company.attendees.count %>
  <%= hidden_field_tag :already_done %>
  <% @company.event.buses.each_with_index do |bus, index| %>
    <h5>
      &emsp;
      <%= check_box_tag "buses[#{bus.id}][]", bus.id, checked = (bus.attendees & @company.attendees).any?, data: { seats: bus.available_seats, overnight: bus.overnight }, class: 'bus-checkbox' %>
      <%= "#{bus.title} - #{bus.available_seats} seats remaining" %>
      <% if (bus.attendees & @company.attendees).any? %>
        (<%= (bus.attendees & @company.attendees).size %> of your attendees)
      <% end %>
    </h5>
  <% end %>
  <h4>
    <%= radio_button_tag "checked_buses", false, checked = session.has_key?(:buses_selected) ? !session[:buses_selected] : false %> NO (We will have our own transportation)
  </h4>

  <div class="buses-error">
    You have elected for transportation to and from the EXPO but have not selected a bus, please choose the appropiate bus before proceeding
  </div>

  <div class="overnight-buses">
    You have elected for an overnight bus for transportation.  This transport election requires that you register for lodging in the following step.  Please remember to book the appropriate lodging
  </div>

  <%= submit_tag 'BACK', class: 'btn btn-primary', name: :step_back %>
  <%= submit_tag 'NEXT', class: 'btn btn-primary next' %>
<% end %>

<div class="modal fade" id="bus-modal" tabindex="-1" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-body">
        <iframe src="http://www.griffins.com/events/expo16.asp#bus" width="1000" height="580" frameborder="0" allowtransparency="true"></iframe> 
      </div>
    </div>
  </div>
</div>
